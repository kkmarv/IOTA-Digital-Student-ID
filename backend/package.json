{
  "name": "digital-student-identity",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -b",
    "dev": "tsnd -r dotenv/config -P ./tsconfig.json --respawn --pretty --transpile-only --clear -- ./src/server/server.ts",
    "start": "npm run build && node -r dotenv/config ./dist/server/server.js"
  },
  "dependencies": {
    "@iota/identity-stronghold-nodejs": "^0.6.0",
    "@iota/identity-wasm": "^0.6.0",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.14",
    "@types/node": "^18.8.3",
    "dotenv": "^16.0.3",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.8.4"
  }
}
