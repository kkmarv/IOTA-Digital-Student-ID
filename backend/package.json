{
  "name": "digital-student-identity",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "dev:server": "nodemon ./src/server/server.ts",
    "start:server": "npm run build && node -r dotenv/config ./dist/server/server.js"
  },
  "dependencies": {
    "@iota/identity-stronghold-nodejs": "^0.6.0",
    "@iota/identity-wasm": "^0.6.0",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.14",
    "@types/node": "^18.8.3",
    "dotenv": "^16.0.3",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  }
}