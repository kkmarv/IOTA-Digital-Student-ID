{
  "watch": ["src", "tsconfig.json", ".env"],
  "ext": "ts",
  "restartable": "r",
  "exec": "node --experimental-specifier-resolution=node --loader ts-node/esm -r dotenv/config",
  "delay": "1000"
}
